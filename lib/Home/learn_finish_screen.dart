import 'package:flutter/material.dart';

class LearnFinishScreen extends StatelessWidget {
  const LearnFinishScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Market',
        style: TextStyle(fontSize: 24.0),
      ),
    );
  }
}